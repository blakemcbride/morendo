# bld output: compiled classes, jars, downloaded dependencies, the compiled build program
/target/
/libs/
/builder/*.class

# Written at runtime by log4j (log4j.properties) and the service package
/logs/
/cache/
