;; Golden scenario: the MOLAP sample (samples/molap). There is no sample data file,
;; so this only characterizes template, cube and rule compilation.
(batch ./samples/molap/templates.clp)
(batch ./samples/molap/cubes.clp)
(batch ./samples/molap/cube_rules.clp)
